@{
  Layout = "_Layout";
}

@model SweetFactory.Models.Flavor;

<h2>Flavor Details</h2>
<hr />
<h3>@Html.DisplayFor(model => model.FlavorName)</h3>

@if(@Model.Treats.Count == 0)
{
  <p>This category does not contain any treats</p>
}
else
{
  <h4>You can get the following treats in this flavor!</h4>
  <ul>
  @foreach(var join in Model.Treats)
  {
    <li>@join.Treat.TreatName</li>
  }
  </ul>
}

<p>@Html.ActionLink("Back to flavors", "Index")</p>
<p>@Html.ActionLink("Delete flavor", "Delete", new { id = Model.FlavorID })</p>